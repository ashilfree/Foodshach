<div class="col-12 col-sm-12 col-md-4 col-lg-3">

    <div class="cardProfile">
        <span></span>
        <img src="{{ '/media/images/avatar-01.jpg' }}" alt="">
        <h5>{{ customer.fullName }}</h5>
    </div>

    <div class="listPro">
        <a href="{{ path('account', { locale: 'ar' }) }}" {% if page is defined and page == 'account' %}class="active-btn"{% endif %}>
            حسابي </a>
        <a href="{{ path('edit.account', { locale: 'ar' }) }}" {% if page is defined and page == 'edit.account' %}class="active-btn"{% endif %}>
            تعديل الملف الشخصي </a>
        <a href="{{ path('my.orders', { locale: 'ar' }) }}" {% if page is defined and page == 'my.orders' %}class="active-btn"{% endif %}>
            طلباتي </a>
        <a href="{{ path('edit.password', { locale: 'ar' }) }}" {% if page is defined and page == 'edit.password' %}class="active-btn"{% endif %}>
            تغيير كلمة المرور </a>
        <a href="{{ path('register', { locale: 'ar' }) }}"> تسجيل خروج </a>
    </div>

    <!-- Modal -->

</div>